[package]
name = "server-monitor-api"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
sysinfo = { version = "0.35.0", features = ["network", "disk", "system"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
